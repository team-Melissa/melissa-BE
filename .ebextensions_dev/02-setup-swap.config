commands:
  setup_swap:
    command: |
      fallocate -l 2G /var/swapfile || dd if=/dev/zero of=/var/swapfile bs=128M count=16
      chmod 600 /var/swapfile
      mkswap /var/swapfile
      swapon /var/swapfile
    test: test ! -e /var/swapfile