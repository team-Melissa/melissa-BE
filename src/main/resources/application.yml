server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/melissa_db?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 12341234
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

openai:
  api:
    key: ${OPENAI_API_KEY:default_openai_api_key}
    url: ${OPENAI_API_URL:https://api.openai.com/v1/chat/completions}